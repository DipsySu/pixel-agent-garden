# 24 — v2.0 Completion Audit

Date: 2026-07-08
Branch: `feat/prd-2.0`
Release tag: `v2.0.0`

This is the post-release audit for the PRD 2.0 goal: finish the documented 2.0
feature set and run three review rounds. It records the evidence used to treat
the goal as closed.

## Release Evidence

- `v2.0.0` points at `467c964 chore: prepare v2.0.0 release`.
- GitHub Release `Local Agent Garden v2.0.0` is published, not draft and not
  prerelease.
- Release assets uploaded:
  - `Local.Agent.Garden_2.0.0_aarch64.dmg`
  - `Local.Agent.Garden_aarch64.app.tar.gz`
  - `Local.Agent.Garden_2.0.0_amd64.AppImage`
  - `Local.Agent.Garden_2.0.0_amd64.deb`
  - `Local.Agent.Garden_2.0.0_x64-setup.exe`
- CI run `28931991464` on `467c964`: rustfmt, web tests, three-platform
  clippy/test, and supply-chain gates all passed.
- Release run `28932129099` on `467c964`: macOS, Linux, and Windows bundle jobs
  all passed.
- Local release gate before tagging also passed:
  - `cargo fmt --all --check`
  - `node --test "web/tests/**/*.test.mjs"` (79 tests)
  - JS syntax checks for `web/*.js` and `web/renderers/*.js`
  - `git diff --check`
  - `cargo test --workspace`
  - `cargo clippy --workspace --all-targets -- -D warnings`
  - `cargo tauri build`

## Feature Matrix

| Requirement | Evidence |
|---|---|
| P1 Tray watch: lit/unlit tray, status row, scan/open controls, launch-at-login setting | `crates/tauri-app/src/tray.rs`, `crates/tauri-app/src/autostart.rs`, `crates/core/src/settings.rs`, `CHANGELOG.md` v1.4 entries |
| P1 Unlock moments and `.is-new` loop | `web/unlock-moments.js`, `web/scene-banner.js`, `web/tests/unlock-moments.test.mjs`, `CHANGELOG.md` v1.4 entries |
| P1 Rings: durable high-water garden memory, UI tab | `crates/core/src/rings.rs`, `crates/core/src/cache.rs`, `web/rings-panel.js`, `web/tests/rings-panel.test.mjs` |
| P2 Agent Nursery | `web/agent-nursery.js`, `web/render-flowerbed.js`, `web/tests/agent-nursery.test.mjs`; v2.0 default is `appearance.flowerbed = "auto"` |
| P3 Weekly recap + Share drawer (incl. new-growth line, see Post-audit corrections) | `web/share-drawer.js`, `web/weekly-card.js`, `web/tests/weekly-card.test.mjs` |
| P3 Seasonal Moment | `web/seasonal-card.js`, scene banner wiring in `web/garden.js`, `web/tests/seasonal-card.test.mjs` |
| P3 Year Review five-card deck (incl. growth timeline, see Post-audit corrections) | `web/year-card.js`, `web/tests/year-card.test.mjs`, scene banner wiring in `web/garden.js` |
| P4 Cost estimate, model/source composition, export | `crates/core/src/prices.rs`, `crates/tauri-app/src/commands.rs`, `web/cost-panel.js`, `web/composition-panel.js`, `web/data-export.js`, `web/tests/cost-estimate.test.mjs`, `web/tests/data-export.test.mjs` |
| P5 First-run reveal, empty-state wood sign, demo mode | `web/first-run.js`, `web/empty-state.js`, `web/data-source.js`, `web/tests/first-run.test.mjs`, `web/tests/empty-state.test.mjs`, `web/tests/data-source.test.mjs` |
| README wall-view explanation | `README.md` references `docs/images/wall-page.jpg` in both English and Chinese sections |
| Privacy/local-only release gates | `.github/workflows/ci.yml`, `deny.toml`, release/CI supply-chain jobs, `PRIVACY.md` |

## Accepted Scope Clarifications

The PRD file is a living product plan and still contains some early wording
around tray menu entry points. The shipped v2.0 interaction model is:

- Weekly / Seasonal / Year artifacts live in the Share drawer.
- Scene banners are the automatic ritual entry point.
- Native tray remains the watch/scan/top-project system surface.
- Year Review exports the complete five-card set as one long PNG. Per-card
  export can be added later if users ask for it.

This matches the latest roadmap and changelog wording. It is not treated as a
release blocker for `v2.0.0`.

## Review Rounds

1. **Architecture and boundary review.** Checked the Rust core / Tauri / web
   boundaries, zero-network contract, source read-only rule, and demo-mode
   isolation. Finding fixed in `97ef0ec`: `loadAdapters()` now stays canned in
   `?demo=1` and has a regression test.
2. **Privacy, state, and compatibility review.** Confirmed the v2.0 release
   uses core-owned tiers/rings/cost math, schema-compatible settings defaults,
   demo gates for local memory and adapter discovery, and pathless exports.
   Existing review fixes are recorded in `CHANGELOG.md` under v1.5-v1.9.
3. **Runtime/UI release review.** Confirmed Share drawer flows, nursery auto
   behavior, empty-state adapter chips, stale comment cleanup, README wall-view
   documentation, local build, CI, and release artifacts.

## Post-audit corrections (2026-07-09)

A follow-up external review found two P3 rows above were marked complete on
structure alone; the ritual artifacts existed but two documented details did
not. Both are now implemented on `feat/year-rings-moments`.

- **P3-3 growth card (PRD line 309: "年轮精选 5 时刻的竖版时间线").** The Year
  Review deck listed `growth` among its five types, but `drawYearDeckCard` had
  no `growth` branch, so it silently fell through to the generic year-overview
  card. It now renders a real vertical timeline of up to five curated ring
  moments (`curateGrowthMoments` prefers milestone events and the earliest
  first-seen, then fills by date; chronological top→bottom). A null/empty book
  (demo, browser, or a quiet year) draws one calm centered line instead of a
  crash or a duplicated overview.
- **P3-1 weekly "new growth" line (PRD lines 277–279: "庭院里新长出的东西").**
  The weekly card rendered tokens and top projects but omitted the new-growth
  line and the dynamic closing. It now lists up to three ring moments that
  landed inside the week and swaps the closing to the "多了一盏灯" line when a
  tier/trinket was gained, falling back to the quiet copy otherwise.

Both consume the core-owned rings book via `loadRings()` (demo/Tauri-gated, so
demo and browser fallback pass `null` and degrade quietly), reuse
`ringEventTitle`/`ringDate` from `web/rings-panel.js`, and render titles/dates
only — never a raw project path or internal key, matching the export-privacy
rule. Coverage added in `web/tests/year-card.test.mjs` and
`web/tests/weekly-card.test.mjs`.

## Final Status

All PRD 2.0 Must/Should items assigned to the v2.0 branch have shipped or have
an explicit accepted scope clarification above. The two P3 detail gaps found in
the post-audit review (above) are now implemented. No release-blocking gap
remains for the original objective.
