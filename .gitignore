__pycache__/
*.py[cod]
.pytest_cache/
.mypy_cache/
.venv/
dist/
build/
*.egg-info/
.DS_Store
review*.png
pixel_garden_organic_vine.html
web/sprite-experiment.html

# Local editor / AI-tool state (per-user, not for the repo)
.claude/

# Claude Design mockup exports — large (3MB+) reference artifacts, kept local.
# (The garden's own UI lives in web/; docs/garden-ui-overview.md describes it.)
docs/design/

# Rust workspace
target/
**/*.rs.bk
web/assets/
crates/tauri-app/gen/
# Cargo.lock is intentionally NOT ignored — workspace has bin crates, so we
# want reproducible builds across machines.
